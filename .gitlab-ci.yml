stages:
  - build
  - test

job_build:
  image: node:latest
  stage: build
  before_script:
    - echo 'build starts'
    - apt-get update
    - apt-get install --no-install-recommends -y npm
    - npm --version
    - npx --version
    - gem install dpl
    - npm install
  script:
    - npm run build
  artifacts:
    paths:
      - ./build

job_test:
  stage: test
  script:
    - echo "Running tests"
