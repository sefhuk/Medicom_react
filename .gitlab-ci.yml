stages:
  - build
  - test

job_build:
  stage: build
  before_script:
    - echo 'build starts'
  script:
    - npm run build
  artifacts:
    paths:
      - ./build

job_test:
  stage: test
  script:
    - echo "Running tests"
